# Getters and Setters Wrapped in Objects

Usually, Java programmers use functions named like `getX()`, `setX(T)` to represent a property of an object. Instead of using them, this project wraps getters and setters in objects to provide a consistent interface for any properties.

WORK STILL IN PROGRESS. COMMING OUT IN A FEW DAYS.

## Building

These files are the simpliest kind of Java files without any dependency. Just use your prefered way of compiling Java. You wouldn't run into any problem.

## Usage

As said, work in progress.
